# Server
base-url: "https://notify.getorganize.app"
behind-proxy: true
log-format: json
manager-interval: 15m
metrics-listen-http: ":9090"

# Access control
auth-file: "/var/cache/ntfy/user.db"
auth-default-access: "deny-all"

# Message cache
cache-file: "/var/cache/ntfy/cache.db"
