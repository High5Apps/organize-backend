services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    volumes:
      - .:/usr/src/app
  certs:
    build:
      context: .
      dockerfile: Dockerfile.certs
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
