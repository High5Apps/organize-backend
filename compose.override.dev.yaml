services:
  app:
    build:
      target: dev
    environment:
      RAILS_ENV: development
    volumes:
      - ./app:/usr/src/app
  certs:
    build:
      context: certs
    environment:
      SELF_SIGN: true
    restart: "no"
  db-upgrade:
    build:
      context: db-upgrade
  web:
    build:
      context: web
