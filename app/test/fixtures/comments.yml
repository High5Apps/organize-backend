one:
  post: one
  user: three
  body: 'Comment body'
  created_at: <%= 10.seconds.from_now.to_fs(:db) %>

two:
  post: one
  user: one
  body: 'Another comment'
  created_at: <%= 11.seconds.from_now.to_fs(:db) %>

three:
  post: two
  user: five
  body: 'Another comment'
  created_at: <%= 12.seconds.from_now.to_fs(:db) %>
