zero:
  ballot: one
  created_at: <%= Time.now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:two, :uuid) %>]
  user: one

one:
  ballot: one
  created_at: <%= 1.second.from_now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:one, :uuid) %>]
  user: one

two:
  ballot: two
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:four, :uuid) %>]
  user: five

three:
  ballot: two
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:two, :uuid) %>]
  user: four

five:
  ballot: one
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:one, :uuid) %>]
  user: three