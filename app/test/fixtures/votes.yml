zero:
  ballot: one
  created_at: <%= Time.now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:two, :uuid) %>]
  user: one

one:
  ballot: one
  created_at: <%= 1.second.from_now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:one, :uuid) %>]
  user: one

two:
  ballot: two
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:four, :uuid) %>]
  user: five

three:
  ballot: two
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:three, :uuid) %>]
  user: four

five:
  ballot: one
  created_at: <%= 2.seconds.from_now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:one, :uuid) %>]
  user: three

six:
  ballot: one
  created_at: <%= Time.now.to_fs(:db) %>
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:two, :uuid) %>]
  user: four

seven:
  ballot: three
  candidate_ids: [<%= ActiveRecord::FixtureSet.identify(:five, :uuid) %>]
  user: four
